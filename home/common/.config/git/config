[user]
  name = eggstrom
  email = tristan65536@gmail.com

[credential]
  username = eggstrom
  # Use git-credential-manager for getting credentials
  helper = manager
  # Use pass for storing credentials
  credentialStore = gpg
  guiPrompt = false

[init]
  defaultBranch = main

[url "https://github.com/"]
  insteadOf = gh:
